# Backend PostgreSQL Prototype Archive

This folder contains the earlier FastAPI and PostgreSQL backend prototype from the integration phase.

It was part of the original project direction, but it is archived here because the final submission was simplified to a static HTML, CSS, and JavaScript demo that does not need a backend server or database setup.
